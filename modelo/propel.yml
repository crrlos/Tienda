propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=tienda
                user: root
                password: admin
                settings:
                    charset: utf8
